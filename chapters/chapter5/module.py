# test_module.py
# ===============================
# 이 파일은 "모듈" 역할을 한다
# import 해서 쓰일 수도 있고
# 직접 실행될 수도 있다
# ===============================


# -------------------------------
# 클래스 정의
# -------------------------------
class Test:
    def __init__(self):
        # 객체가 생성될 때 자동으로 실행됨 (생성자)
        print("Test 객체 생성됨")

    def hello(self):
        print("hello from class")


# -------------------------------
# 함수 정의
# -------------------------------
def say_hello():
    # 함수는 호출해야 실행됨
    print("hello from function")


def add(a, b):
    # a, b는 매개변수(parameter)
    return a + b


# -------------------------------
# 이 파일을 '직접 실행'했을 때만 실행되는 영역
# -------------------------------
if __name__ == "__main__":
    print("📌 test_module.py를 직접 실행함")

    # 클래스 사용
    t = Test()
    t.hello()

    # 함수 사용
    say_hello()
    result = add(3, 5)
    print("add 결과:", result)
